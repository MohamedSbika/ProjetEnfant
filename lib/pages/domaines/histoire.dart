import 'package:flutter/material.dart';
class HistoirePage extends StatelessWidget {
  const HistoirePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {

    return Scaffold();
  }
}